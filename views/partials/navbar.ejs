<header class="cabecalho-principal">
    <div class="container d-flex align-items-center justify-content-between py-2">
     

        <nav class="navegacao-links d-none d-lg-flex align-items-center gap-4">
            <% if (user) { %>
                <a href="/<%= user.role === 'administrador' ? 'admin' : user.role === 'supermercados' ? 'supermercado' : user.role === 'estafetas' ? 'estafeta' : 'cliente' %>/dashboard" class="link-menu text-decoration-none">Início</a>
                
                <% if (user.role === 'supermercados') { %>
                    <a href="/supermercado/produtos" class="link-menu text-decoration-none">Gerir Produtos</a>
                    <a href="/supermercado/encomendas" class="link-menu text-decoration-none">Encomendas</a>
                <% } %>

                <% if (user.role === 'estafetas') { %>
                    <a href="/estafeta/entregas" class="link-menu text-decoration-none">Entregas</a>
                <% } %>

                <% if (user.role === 'administrador') { %>
                    <a href="/admin/utilizadores" class="link-menu text-decoration-none">Utilizadores</a>
                <% } %>
            <% } %>
        </nav>

        <div class="area-utilizador d-flex align-items-center gap-3">
            <% if (user) { %>
                <% 
                    // Definir o link de perfil dependendo da role
                    let urlPerfil = "/cliente/perfil"; 
                    if (user.role === 'supermercados') urlPerfil = "/supermercado/perfil";
                    else if (user.role === 'estafetas') urlPerfil = "/estafeta/perfil";
                    else if (user.role === 'administrador') urlPerfil = "/admin/perfil";
                %>
                
                <!-- Nome/Perfil Dinâmico -->
                <a href="<%= urlPerfil %>" class="text-decoration-none text-dark small fw-bold d-none d-md-inline">
                    <i class="bi bi-person-circle me-1 text-primary"></i> <%= user.nome %>
                </a>
                
                <span class="text-muted d-none d-md-inline">|</span>

                <a href="/auth/logout" class="btn btn-outline-danger btn-sm fw-bold px-3" style="border-radius: 4px;">
                    <i class="bi bi-box-arrow-right me-1"></i> Sair
                </a>
            <% } else { %>
                <a href="/auth/login" class="btn btn-primary btn-sm fw-bold px-4" style="border-radius: 4px;">Entrar</a>
            <% } %>
        </div>
    </div>
</header>
